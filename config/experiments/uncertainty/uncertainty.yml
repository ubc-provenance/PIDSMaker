training_loop:
  run_evaluation: each_epoch

experiment:
  used_method: uncertainty
  uncertainty:
    hyperparameter:
      hyperparameters: lr, num_epochs, text_h_dim, gnn_h_dim
      iterations: 5
      delta: 0.2
    mc_dropout:
      iterations: 10
      dropout: 0.4
    deep_ensemble:
      iterations: 10
    bagged_ensemble:
      iterations: 5
      min_num_days: 3
