training_loop:
  run_evaluation: best_epoch

experiment:
  used_method: uncertainty
  uncertainty:
    hyperparameter:
      hyperparameters: lr, num_epochs, text_h_dim, gnn_h_dim
      iterations: 5
      delta: 0.2
