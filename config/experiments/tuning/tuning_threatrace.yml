method: grid
metric:
  name: val_score
  goal: maximize
count: 16

parameters:
  detection.gnn_training.lr:
    values: [0.01, 0.001, 0.0001]
  detection.gnn_training.node_hid_dim:
    values: [32, 128]
  detection.gnn_training.edge_batch_size:
    values: [0, 3000]

wandb_project: tuning_threatrace
