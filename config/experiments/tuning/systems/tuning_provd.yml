method: grid
metric:
  name: val_score
  goal: maximize

parameters:
  featurization.embed_nodes.emb_dim:
    values: [100, 200]
  preprocessing.build_graphs.time_window_size:
    values: [5.0, 10.0, 15.0]
